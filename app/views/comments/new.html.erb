

                <%= form_for (@comment) do |f| %>
                  <div class="form-group">
                  <%= f.label :content %>
                  <%= f.text_area :content, cols: 20, rows: 10, class: 'form-control' %>
                  
                </div>
  
                <div class="form-group">
                    <%= f.submit :submit %>
                </div>
                <% end %>







  